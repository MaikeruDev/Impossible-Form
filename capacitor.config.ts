import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Impossible Form',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
